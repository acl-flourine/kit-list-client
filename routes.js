page('/', app.listView.initHomePage);

// page('/home', app.listView.initHomePage);

// page('/:user',     ); //When user clicks already a user button, get items from join table, call toHtml

// page('/about',     );

page.start();